# READ THIS AI — تعليمات معمارية إلزامية للمشروع

> هذا الملف موجّه **لأي ذكاء اصطناعي** (Grok / ChatGPT / Claude / Cursor / etc.) يشتغل على هذا المشروع.
> اقرأه كاملًا قبل أي تعديل.

---

## 0) تعريف كلمة «شامل» — اقرأ هذا أولًا (إلزامي)

عندما يقول المستخدم **«شامل»** أو **«الملف الشامل»** أو **«ملف الشامل»** فالمقصود **حصرًا**:

> **ملف ZIP الذي يُحمَّل (Download) ويُرفع (Upload) من وضع المطوّر / وضع البناء داخل اللعبة.**

### ما هو؟
- ناتج زر **⬇️ تحميل** في شريط أدوات **وضع البناء (Build Mode)**.
- نفس الملف الذي يُرفع من شاشات: إنشاء غرفة / انضمام / قائمة الملفات في وضع المطوّر.
- اسمه النموذجي: `story_mode_data_....zip` أو أي ZIP يسمّيه المستخدم «الشامل».

### ماذا يحتوي؟
محتوى المستويات فقط: `build.json` + سكربتات + أصوات + أغاني + صور (مجلد `levels/...`).

### ماذا **ليس** هو؟
- **ليس** حزمة تشغيل الموقع (`index.html` + `main.js` + `three.min.js` + …).
- **ليس** ملف اللعبة الكامل الذي يُرسل للاعب ليشغّل المتصفح.
- **ليس** مجرد «كل ملفات المشروع».

### لو طلب المستخدم تصغير/ضغط/إصلاح «الشامل»:
عدّل منطق التصدير/الاستيراد في الموقع (`downloadAllAsZip` / `processZipArrayBuffer`) أو محتوى اللفل داخل الـ ZIP — **لا** تخلطه بحزمة Runtime.

```
شامل = ملف بيانات المستويات (تحميل/رفع من وضع البناء والمطوّر)
ملف الموقع = محرّك اللعبة والواجهة
```

---

## 1) الفكرة الأساسية (مهم جدًا)

المشروع مكوّن من **نوعين منفصلين** من الملفات:

| النوع | ماذا هو؟ | ماذا يُعدَّل فيه؟ |
|--------|-----------|-------------------|
| **ملف الموقع (Website / Runtime)** | محرّك اللعبة، الواجهة، الشبكة، البناء، Firebase/LAN | البنية التحتية فقط — **ليس** برمجة اللفل |
| **الملف الشامل (Comprehensive Pack)** | محتوى اللفل: برمجة، أغاني، أصوات، بناء، صور — يُحمَّل ويُرفع من وضع البناء/المطوّر | **هنا** تتم برمجة اللفل والأحداث |

### القاعدة الذهبية

```
برمجة داخل اللفل  →  الملف الشامل فقط
ملف الموقع الأصلي →  لا يُمسّ من أجل سكربتات/أحداث اللفل
```

- **الملف الشامل** = ما يُحمَّل من زر التحميل في وضع البناء ويُرفع في وضع المطوّر/اللوبي (scripts / music / sounds / build data).
- **ملف الموقع** = المنصة التي تشغّل أي شامل يُرفع إليها.

لا تخلط بينهما. لا تضع منطق اللفل داخل `main.js` الخاص بالموقع إلا إذا كان طلبًا صريحًا عن البنية التحتية (شبكة، بناء، قائمة، Firebase…).

---

## 2) ملف الموقع (Website)

### أمثلة على الأسماء
- `story-mode-fixed-full.zip`
- مجلد فيه: `index.html`, `main.js`, `style.css`, `lan_host.py`, `pack/`, …

### ماذا يفعل؟
- قائمة رئيسية، لوبي، أونلاين (LAN / Firebase)
- وضع البناء (Build Mode)
- تحميل/رفع الملف الشامل
- تشغيل اللفل بعد اختياره
- شبكة اللاعبين، المزامنة، الشخصيات، الكاميرا
- Game API التي يستدعيها سكربت اللفل (`Game.netSend`, `Game.toast`, `Game.getPlayer`, …)

### متى تعدّل ملف الموقع؟
فقط عند طلب يتعلق بـ:
- الشبكة / LAN / Firebase / غرف الأونلاين
- واجهة القوائم أو البناء
- محرّك اللعب العام (ليس حدثًا خاصًا بلفل معيّن)
- إصلاح ظهور اللاعب التالت، المزامنة العامة، الريسبون العام، إلخ

### متى **لا** تعدّل ملف الموقع؟
- إضافة عربيات / أغاني / كاتسين في لفل
- سكربت intro أو أحداث القصة
- تليفون داخل اللفل، موسيقى اللفل، أصوات اللفل
- أي “برمجة لفل” بالمعنى التصميمي

---

## 3) الملف الشامل (Comprehensive Pack)

### أمثلة على الأسماء
- `الملف الشامل.zip`
- `الملف_الشامل_محدث_مصلح.zip`

### البنية النموذجية (قد تختلف الأسماء قليلًا)
```
manifest.json
levels/                 (أو الشامل/)
  لفل 1_xxxx/
    البناء/             build.json
    البرمجيات/          intro_cutscene.js   ← سكربت اللفل
    الاغاني/            *.mp3
    الاصوات/            *.wav
    الصور/
الشامل/                 (نسخة مجمّعة أحيانًا)
  ...
```

### ماذا يُعدَّل هنا؟
- **البرمجة داخل اللفل** (`intro_cutscene.js` أو أي سكربت مستوى)
- الأغاني، الأصوات، عناصر مرتبطة باللفل
- بيانات البناء الخاصة بالمستوى إن كانت داخل الشامل

### كيف يعمل مع الموقع؟
1. المطوّر/اللاعب يفتح **ملف الموقع**
2. يرفع **الملف الشامل** من الواجهة
3. الموقع يقرأ السكربتات والأصول من الشامل ويشغّلها عبر `Game` API
4. تعديلات القصة/اللفل تُحفظ وتُوزَّع كـ **شامل جديد** — بدون تغيير محرّك الموقع

---

## 4) قواعد عمل للـ AI (Checklist)

قبل أي تغيير اسأل نفسك:

1. **هل الطلب عن حدث/منطق داخل اللفل؟**
   - نعم → عدّل سكربت/أصول **الملف الشامل** فقط.
2. **هل الطلب عن المنصة (قائمة، شبكة، بناء عام، Firebase)؟**
   - نعم → عدّل **ملف الموقع** فقط، بحذر.
3. **هل الطلب غامض؟**
   - افترض أن برمجة اللفل = الشامل، إلا إذا ذُكر صراحة “الموقع” أو `main.js`.
4. **لا تدمج الشامل داخل `main.js`** كمنطق لفل دائم.
5. **عند إخراج نتيجة للمستخدم:**
   - إذا عدّلت الشامل → أرجع ZIP الشامل المحدَّث.
   - إذا عدّلت الموقع → أرجع ZIP الموقع المحدَّث.
   - إذا الاثنين مطلوبين صراحة → أرجعهما منفصلين ووضّح الفرق.

---

## 5) أمثلة سريعة

| طلب المستخدم | أين التعديل؟ |
|--------------|----------------|
| “ضيف 3 عربيات في لفل 1” | الشامل → سكربت اللفل |
| “خلي فيه تليفون ورنة آيفون” | الشامل → سكربت اللفل |
| “اربط الأونلاين بـ Firebase” | الموقع → `main.js` / `index.html` |
| “زر اختبار في وضع البناء” | الموقع → واجهة البناء |
| “صلّح مزامنة اللاعب التالت” | الموقع (شبكة) + أحيانًا تنسيق رسائل السكربت في الشامل |
| “ضيف أغاني للفولدر” | الشامل → مجلد الأغاني |

---

## 6) جملة واحدة للتذكير

**الملف الشامل = محتوى وبرمجة اللفل.**  
**ملف الموقع = المحرّك والمنصة — لا تُبرمج فيه قصة اللفل.**

أي AI يقرأ هذا الملف يجب أن يحترم هذا الفصل في كل التعديلات القادمة.

---

*Generated for project maintainers and coding agents. Keep this file next to the zips when handing work to an AI.*


---

## 5) اللفل المصمَّم قابل للبرمجة (مهم للمصمّم والـ AI)

اللفل اللي اللاعب/المصمّم بيبنيه في **وضع البناء** مش ديكور بس — **كل كائن فيه قابل للبرمجة**.

### إزاي المصمّم يحدد إيه يتبرمج؟

1. في وضع البناء تظهر **قائمة الكائنات (الهرمية / Hierarchy)**.
2. كل عنصر ليه:
   - **اسم** (مثل: منصة، حائط، مرسيدس جي كلاس…)
   - **رقم / ترتيب** في القائمة (مثل: منصة رقم 4، حائط رقم 2)
3. لما المصمّم يطلب برمجة من الـ AI يقول بصيغة واضحة، مثلاً:

```
برمجلي المنصة رقم 4
لما اللاعب يقف عليها تفتح الباب اسمه "باب 2"
```

أو:

```
العنصر: عربية "جي كلاس" رقم 1
البرمجة: لما أضغط F أركب، والبنزين ينقص مع السرعة
```

### صيغة الطلب المفضّلة للـ AI

| الحقل | مثال |
|--------|------|
| **الاسم** | منصة / حائط / جي كلاس / مضخة بنزين |
| **الرقم** | 4 (الرابع في قائمة نفس النوع أو في الهرمية) |
| **الحدث** | لما اللاعب يقرب / يضغط E / يدخل العربية |
| **النتيجة** | رسالة، باب يفتح، نقطة تتحول، سكربت يبدأ |

### أين تُكتب البرمجة؟

- في **الملف الشامل** → سكربت اللفل (مثل `intro_cutscene.js` أو سكربت المستوى).
- يُفضَّل الربط بالاسم/`instanceName` أو رقم ثابت يتفق عليه المصمّم.
- **لا** تضع منطق خاص بلفل معيّن داخل `main.js` للموقع إلا لو طلب صريح عن المحرّك العام.

### أمثلة ربط برمجي

```js
// ابحث عن كائن بالاسم الظاهر في الهرمية
function findByName(namePart) {
  var list = Game.getBuildObjects ? Game.getBuildObjects() : [];
  for (var i = 0; i < list.length; i++) {
    var n = (list[i].userData && list[i].userData.instanceName) || '';
    if (n.indexOf(namePart) >= 0) return list[i];
  }
  return null;
}

// مثال: المنصة رقم 4 → غالبًا اسمها "منصة 4" أو رابع منصة
var plat = findByName('منصة 4');
```

### مسؤولية الـ AI

- لما المصمّم يقول **الاسم + الرقم** → اعتبرها مواصفة برمجة ملزِمة.
- نفّذ البرمجة في **الشامل** واربط بالكائن الصحيح.
- لو الاسم/الرقم غير واضح، اسأل قبل التعديل.
- بعد البناء، الحفظ التلقائي في الموقع يحفظ مواقع الكائنات؛ البرمجة تظل في سكربت الشامل.

---

## 6) ملخص سريع

| طلب المصمّم | أين التعديل؟ |
|-------------|---------------|
| شبكة / لوبي / بناء عام / كاميرا عامة | ملف الموقع |
| حدث لفل، عربية قصة، أغنية، تليفون داخل اللفل | الملف الشامل |
| «برمج المنصة رقم 4…» | الشامل + ربط بالاسم/الرقم من الهرمية |

اللفل المصمَّم = **قابل للبرمجة بالكامل** عبر تسمية الكائنات وأرقامها في قائمة البناء.


---
